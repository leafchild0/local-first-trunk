## Description

Shared module with types/interfaces for shared data bindings.

## Project setup

```bash
$ pnpm install
```

## How to use

Use either npm or pnpm workspaces. Refer to a root of the repo for an example.
Later use it as a dependency like this:

```json
"@lft/shared": "workspace:*",
```